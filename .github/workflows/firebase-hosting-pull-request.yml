# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
'on': pull_request
jobs:
  build_and_preview:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: create-environment-prod
        uses: "finnp/create-file-action@master"
        env:
          FILE_NAME: "projects/the-application/src/environments/environment.prod.ts"
          FILE_DATA: ${{ secrets.FIREBASE_ANGULAR_ENVIRONMENT }}
      - name: create-environment
        uses: "finnp/create-file-action@master"
        env:
          FILE_NAME: "projects/the-application/src/environments/environment.ts"
          FILE_DATA: ${{ secrets.FIREBASE_ANGULAR_ENVIRONMENT }}
      - name: create-firebaserc
        uses: jsdaniell/create-json@1.1.2
        with:
          name: ".firebaserc"
          json: ${{ secrets.FIREBASE_ANGULAR_RC }}
          dir: "."
      - name: install-and-build
        run: 'npm ci && npm run build:shell'
      - name: firebase
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_TEDAKY_GAMES }}'
          projectId: tedaky-games
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
